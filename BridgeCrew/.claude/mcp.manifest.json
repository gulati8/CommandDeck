{
  "mcpServers": {
    "playwright": {
      "source": "claude-plugins-official",
      "plugin": "playwright",
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "notes": "Install via /plugin install playwright@claude-plugins-official; do not auto-edit user-level MCP config from project scripts."
    }
  }
}
